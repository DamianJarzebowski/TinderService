databaseChangeLog:

  -  changeSet:
       id: 0001
       author: Damian Jarzębowski
       changes:

         - createTable:
             tableName: developers

             columns:

               - column:
                   name: id
                   type: BIGINT
                   autoIncrement: true

                   constraints:
                     primaryKey: true
                     nullable: false

               - column:
                   name: first_name
                   type: varchar(30)
                   constraints:
                     nullable: false

               - column:
                   name: last_name
                   type: varchar(30)
                   constraints:
                     nullable: false

               - column:
                   name: description
                   type: varchar(500)

               - column:
                   name: profession
                   type: varvhar(30)

